# Signing Key Generator

{/* HeroSection: Main landing area with title and description */}
<HeroSection>
# Signing Key Generator

Generate memorable cryptographic signing key pairs with dictionary word patterns. Keys are scored based on readability and pattern matching.
</HeroSection>

{/* InstructionsSection: Explains how the system works */}
<InstructionsSection>
## How it works

### Scoring System
- **Normal:** Score ≤ 1024 (Gray)
- **Magic:** Score > 1024 (Blue) 
- **Rare:** Score > 2048 (Yellow)
- **Legendary:** Score > 4096 (Orange)

### Features
- Real-time key generation
- Dictionary word matching
- Click any key to copy
- Best keys displayed first
</InstructionsSection>

{/* KeyGeneratorSection: The main key generation interface */}
<KeyGeneratorSection>
## Generated Signing Keys

Showing the best signing keys found so far. Click any key to copy its details.

The system continuously generates cryptographic signing key pairs and displays them in real-time, sorted by their memorability score. Keys with higher scores contain more recognizable dictionary words and patterns.
</KeyGeneratorSection>

{/* SecurityNotice: Important security information */}
<SecurityNotice>
Keys are generated using cryptographically secure methods. Pattern matching does not compromise security.
</SecurityNotice> 